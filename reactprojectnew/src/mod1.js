
/*
// Define a class Point with x and y as data members
class Point { //first letter is capital
    constructor(x, y) { //constructor
        this.x = x;
        this.y = y;
    }
    display() {
        console.log("Point x: "+this.x+" y: "+this.y);
    }
}
// Define a class Point3D
export class Point3D extends Point {
    constructor(x, y, z) {
        super(x, y);
        this.z = z;
    }
    display() {
        super.display();
        console.log("Point3d z: "+this.z);
        console.log("Point3d z: "+this.z+" x: "+this.x);
    }
}
export default Point; //default export
*/